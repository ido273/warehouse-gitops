# apps/backend.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backend
  namespace: argocd
spec:
  project: default
  sources:
    - repoURL: https://github.com/ido273/warehouse-app.git
      targetRevision: master
      path: backend/helm
      helm:
        valueFiles:
          - values.yaml
          - $values/envs/production/backend-values.yaml
    - repoURL: https://github.com/ido273/warehouse-gitops.git
      targetRevision: master
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: warehouse
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true